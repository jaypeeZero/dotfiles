#!/usr/bin/env bash
#
# Shell options that were previously stranded in dotfiles/.bashrc, so only
# macOS got them. Tracked here, every platform gets them via the loader.

# vi mode for command-line editing
set -o vi

export EDITOR=vim
