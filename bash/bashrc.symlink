#!/bin/bash

export DF=$HOME/.df

# Find all configuration files
config_files=($DF/**/*.bash)

#echo $config_files
#echo ${config_files[*]}

echo ${config_files[2]}
echo "---"

# Source all of the path ones first
for file in ${config_files[*]#*/path.bash}
do
  echo $file
done

