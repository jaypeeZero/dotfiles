.DS_Store
vim.symlink/backup
vim.symlink/.netrwhist
**/*.pem

dotfiles/.config/*
!dotfiles/.config/nvim/
# Secrets. Deliberately broad — any name containing "env", any extension.
*env*
.secrets

# ...but these are shell config that merely contains "env" in the name.
!bash_includes/pyenv.bash
!bash_includes/venv-cd.bash
